import { createClient } from "@/lib/supabase/server";

export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const supabase=await createClient();const {data:r}=await supabase.from("reports").select("*,findings(title,severity,programmes(name),assets(value))").eq("id",id).single();if(!r)return new Response("Not found",{status:404});const f=r.findings;const sections=[["Summary",r.summary],["Steps to Reproduce",r.steps],["Expected Behaviour",r.expected_behaviour],["Actual Behaviour",r.actual_behaviour],["Impact",r.impact],["Evidence",r.evidence_text],["Suggested Remediation",r.suggested_remediation]];const body=`# ${r.title}\n\n- **Programme:** ${f?.programmes?.name||""}\n- **Affected asset:** ${f?.assets?.value||"Not recorded"}\n- **Severity:** ${f?.severity||""}\n${sections.map(([h,c])=>`\n## ${h}\n\n${c||"_Not yet completed._"}\n`).join("")}`;return new Response(body,{headers:{"Content-Type":"text/markdown; charset=utf-8","Content-Disposition":`attachment; filename="scopeguard-report-${id}.md"`}})}
